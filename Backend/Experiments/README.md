# Backend/Experiments

All Jupyter notebooks and experimental work (JSON schemas, temporary files, data exploration) should live here.

- Keep notebooks well-documented and version-controlled.
- Avoid storing large datasets directly in this folder; reference external storage instead.
