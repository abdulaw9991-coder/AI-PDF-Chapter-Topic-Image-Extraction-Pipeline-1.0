# Backend/App

Place all Python application code inside this folder. Organize code into subpackages, for example:

- `src/` — main package modules
- `scripts/` — small one-off scripts or CLI utilities
- `tests/` — unit and integration tests

Keep this folder clean: do not store data, notebooks, or large binaries here.
